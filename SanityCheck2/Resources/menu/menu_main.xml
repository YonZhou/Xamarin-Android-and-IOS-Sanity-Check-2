<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <item
        android:id="@+id/action_search"
        android:orderInCategory="100"
        android:title="@string/action_search"
        app:showAsAction="never" />
  <item
        android:id="@+id/stampButton"
        android:orderInCategory="200"
        android:title="Stamp Nuclear"
        app:showAsAction="never"/>

  <item
        android:id="@+id/saveAnnotationsButton"
        android:orderInCategory="300"
        android:title="Save Annotations"
        app:showAsAction="never"/>

  <item
        android:id="@+id/loadAnnotationsButton"
        android:orderInCategory="300"
        android:title="Load Annotations"
        app:showAsAction="never"/>

  <item
      android:id="@+id/compareFilesButton"
      android:orderInCategory="400"
      android:title="Compare Files"
      app:showAsAction="never"/>

</menu>
